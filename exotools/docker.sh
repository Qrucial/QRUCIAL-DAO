#!/bin/bash
#SCRIPT_PATH=$(dirname $(realpath "${BASH_SOURCE:-$0}"))
#echo $SCRIPT_PATH
# This should maybe be responsible for:
#   > running the correct docker container,
#   > Creating the images from the docker-files.
#   > mounting to the correct directory.
